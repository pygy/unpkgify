{
  "name": "esm-unpkgify",
  "type": "module",
  "version": "0.0.0",
  "description": "Takes JS source code, and turns raw ES module specifiers into unpkg urls.",
  "main": "index.js",
  "scripts": {
    "build": "node ./scripts/build-regexp.js",
    "test": "gosub build && ospec tests/tests.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "compose-regexp": "^0.7.1",
    "gosub": "^2.0.0",
    "ospec": "^4.2.0"
  }
}
